<!DOCTYPE html>
<html>
  <head>
    <%= render "layouts/head" %>
    <%= render 'magic_test/support' if Rails.env.test? %>
  </head>

  <body>
    <%= render "shared/navbar" %>
    <div class="min-h-[calc(100vh-4rem)] w-full flex flex-col items-center justify-center">
      <%= yield %>
    </div>
    <%= render "shared/flashes" %>
  </body>
</html>
